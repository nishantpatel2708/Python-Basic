                ## Abstraction

## Abstraction is a proccess of hiding the implementation details from the user only highlighted set of services provided to user.

## Abstraction is used to hide the internal functionality of the function from the users. The users only interact with the basic implementation of the function, but inner working is hidden. User is familiar with that "what function does" but they don't know "how it does."


import first1, second2

first1.Add()
second2.sub()


                # abstract class

    # abstract class is a class that contains one or more abstract methods is called abstract class.

### NOT UNDERSTOOD

                # interface 
    # interface  is nothing but abstract class which contains only abstract method but not any normal method.
    